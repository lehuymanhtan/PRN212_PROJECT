<Window x:Class="AIStudyHub.Views.SubjectSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chọn Môn Học" Height="220" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource AppBgBrush}" FontFamily="{StaticResource AppFont}">
    <Border Style="{StaticResource CardStyle}" Margin="16">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Vui lòng chọn môn học cho tài liệu này:" 
                       Style="{StaticResource H2Style}" Margin="0,0,0,12"/>

            <ComboBox x:Name="SubjectComboBox" Grid.Row="1" Height="36" Padding="8,4" FontSize="14"
                      DisplayMemberPath="Name">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,16,0,0">
                <Button Content="Hủy" Click="CancelButton_Click" IsCancel="True"
                        Padding="16,8" Margin="0,0,10,0" Background="{StaticResource BorderBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" Cursor="Hand"/>
                <Button Content="Xác nhận" Click="SelectButton_Click" IsDefault="True"
                        Style="{StaticResource PrimaryButtonStyle}" Padding="16,8"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
