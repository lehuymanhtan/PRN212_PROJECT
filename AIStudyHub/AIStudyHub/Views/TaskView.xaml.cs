using System.Windows.Controls;

namespace AIStudyHub.Views
{
    /// <summary>
    /// Code-behind cho TaskView.xaml.
    /// Theo chuẩn MVVM, file này chỉ chứa InitializeComponent().
    /// Toàn bộ logic nghiệp vụ được xử lý tại TaskViewModel.cs.
    /// </summary>
    public partial class TaskView : UserControl
    {
        public TaskView()
        {
            InitializeComponent();
        }
    }
}
